# Используем официальный облегчённый образ Node.js на базе 
FROM node:18-alpine 
# Устанавливаем рабочую директорию внутри контейнера 
WORKDIR /app 
# Копируем только package.json (чтобы закешировать слои с 
COPY package.json ./ 
# Устанавливаем зависимости 
RUN npm install 
# Копируем оставшиеся файлы проекта в контейнер 
COPY . . 
# Указываем порт, который будет использовать приложение внутри  
EXPOSE 3000 
# Команда, запускающая приложение 
CMD ["node", "server.js"]